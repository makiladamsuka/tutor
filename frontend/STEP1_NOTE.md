# Step 1 migration note

The previous **Next.js** App Router UI (`src/app/`), demo components (`src/components/`), and Zustand store were **removed** when switching to **Vite + `@crxjs/vite-plugin`**.

Recover them from Git history if you need to port Beyond Presence / layout code into `src/sidepanel/` in later playbook steps.
